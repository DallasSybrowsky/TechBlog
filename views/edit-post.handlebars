<div>
  <a href="/dashboard" >Back to your dashboard</a>
  <h2 class="title mt-5">Edit a post</h2><hr>
  <form class="edit-post-form">
    <div>
        <label for="post-title">Post Title:</label>
      <input class="input" name="post-title" type="text" value="{{post.title}}" />
    </div>
    <div>
        <label for="post-content">Post content:</label>
      <input class="textarea" name="post-content" type="text" value="{{post.post_content}}" />
    </div>
    <div>
       created by you on {{format_date post.created_at}}
      <a href="/post/{{post.id}}">{{post.comments.length}} {{format_plural "comment" post.comments.length}}</a>
    </div>
    <button type="submit" class="btn">Save your post</button>
    <button type="button" class="btn-danger">Delete post</button>
  </form>
</div>

{{> comments post.comments}}

<script src="/js/edit.js"></script>
<script src="/js/"></script>
<script src="/js/comment.js"></script>
